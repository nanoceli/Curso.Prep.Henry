Array: 

Es un conjunto de datos colocado entre corchetes, y dentro tiene posiciones o 'casilleros' donde se ubicara cada elemento, que puede ser un caracter solo o directamente una funcion, o otro array, por ejemplo.